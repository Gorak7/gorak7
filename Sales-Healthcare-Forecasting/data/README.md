# Dataset

Place the project dataset in this directory as `data.csv`.

Expected minimum columns:

- `Date` — observation date
- `Value` — numeric sales, demand, visits, cases, or another measurable quantity

For a public portfolio repository, use an open dataset or synthetic/de-identified data and document its source here.
